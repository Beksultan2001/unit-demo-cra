name: Issue
run-name: issue
on: [workflow_call]

jobs:
  issue:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write

    outputs:
      issue_number: ${{ steps.create_issue.outputs.result }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install auto-changelog
        run: npm i -g auto-changelog
        
      - name: Generate changelog
        run: auto-changelog --commit-limit false --template keepachangelog

      - name: Create Issue
        id: create_issue
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
          
            const fs = require('fs');
            const changeLog = fs.readFileSync('CHANGELOG.md', 'utf8');
            const MAX_BODY_LENGTH = 4000;

            // Truncate the changeLog if it exceeds the maximum length
            const truncatedDescription = changeLog.length > MAX_BODY_LENGTH
              ? changeLog.slice(0, MAX_BODY_LENGTH)
              : changeLog;

            const [mainOwn, mainRepo] = '${{ github.repository }}'.split('/');
            const refVersion = '${{ github.ref_name }}';
            const value = `Release created ${refVersion}`;
            const temp = { mainOwn, mainRepo, labels: ['RELEASE'] };
            const list = await github.rest.issues.listForRepo({ ...issue, state: 'all' });
            const isThere = list.data.find(i => i.title === value);
            if (isThere) {
              const update = { ...temp, issue_number: isThere.number, body: truncatedDescription, state: 'open' };
              await github.rest.issues.update(update);
              return isThere.number;
            } else {
              const create = { ...temp, title: value, body: truncatedDescription };
              const response = await github.rest.issues.create(create);
              return response.data.number;
            }

            
